# Linux reference files

Linux originals were not present on this Mac. Recreate from [docs/archive/SPEC.md](../docs/archive/SPEC.md) §§4–6.

- `config.toml` — see `../config/config.toml` (Mac-adapted)
- `voxtype-clean` — see `../bin/voxtype-clean`
- `Popcorn.qml` — SPEC §6.5 tunables are binding; heap reconstructed (see [docs/archive/NOTES.md](../docs/archive/NOTES.md))
- `voxtype-osd.toml` — not required on Mac (built-in OSD disabled)
