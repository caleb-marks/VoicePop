# Visual captures

- `recording.png` / `transcribing.png` — full-screen captures while forcing `/tmp/voxtype/state` (daemon stopped briefly). Inspect bottom-center bag / capsule at actual size.
- Re-capture after display changes: force state, then `screencapture -x docs/visuals/...`.
